---
title: "目标流水线"
description: "让执行者反复工作，直到验证者认可结果"
---

单个智能体交出的第一版结果不一定合格。`GoalPipeline` 把「做」和「验」拆成两个智能体，让它们循环起来：

```
        ┌────────── 不通过：带着理由退回重做 ──────────┐
        │                                          │
        ▼                                          │
输入 ─▶ 执行者 ────── 产出 ──────▶ 验证者 ────────────┘
                                    │
                                    └── 通过 ──▶ 结束
```

**验证者是一个普通的 `Agent`**，不是某种特殊对象 —— 它的结论通过[结构化输出](/versions/2.0.8dev/zh/building-blocks/agent/run-agent)给出，所以一个需要读文件、跑命令、甚至请人确认的验证，走的是和执行完全相同的机制。

## 运行一条流水线

```python
import asyncio
import os

from agentscope.agent import Agent
from agentscope.console import launch_console
from agentscope.credential import DashScopeCredential
from agentscope.model import DashScopeChatModel
from agentscope.pipeline import GoalPipeline
from agentscope.tool import Toolkit
from agentscope.workspace import LocalWorkspace


async def main() -> None:
    # 两个智能体共享同一个工作区，验证者才能看到执行者真正写出来的
    # 东西，而不是它声称写了什么
    async with LocalWorkspace(workdir="./workspace") as workspace:
        model = DashScopeChatModel(
            credential=DashScopeCredential(
                api_key=os.getenv("DASHSCOPE_API_KEY"),
            ),
            model="qwen3.8-max",
        )

        # 执行者：干活的那个，用工作区的文件工具写代码
        executor = Agent(
            name="Executor",
            system_prompt="You're a programmer named 'Executor'.",
            model=model,
            toolkit=Toolkit(tools=await workspace.list_tools()),
            offloader=workspace,
        )

        # 验证者：判定的那个。装同一套工具，因此它能真的去读代码、
        # 跑测试，而不是只凭执行者的自述下结论
        verifier = Agent(
            name="Verifier",
            system_prompt="You're a reviewer named 'Verifier'.",
            model=model,
            toolkit=Toolkit(tools=await workspace.list_tools()),
            offloader=workspace,
        )

        pipe = GoalPipeline(
            executor=executor,
            verifier=verifier,
            # goal 是给验证者的判据，不是给执行者的任务
            goal="""检查执行者写的代码，判断是否合格：
1. 必须是 Python。
2. 必须包含 requirements.txt。
3. 代码能无报错运行。
4. 结构清晰，符合常见实践。""",
            # 最多被驳回 5 次，之后即便未通过也停止
            max_iters=5,
        )

        # 流水线满足 PipelineProtocol，可以直接交给终端 UI
        await launch_console(agent=pipe)


asyncio.run(main())
```

### 构造参数

| 参数 | 类型 | 说明 |
|------|------|------|
| `executor` | `Agent` | 干活的智能体 |
| `verifier` | `Agent` | 判定的智能体 |
| `goal` | `str \| list[TextBlock \| DataBlock]` | 验收标准。用 block 列表可以带上截图、示意图等 |
| `max_iters` | `int`，默认 `10` | 最多允许被驳回几次 |

<Note>
`goal` 是**给验证者的判据**，不是给执行者的任务描述。任务从 `reply_stream` 的输入进来，验收标准由 `goal` 固定，两者分开。
</Note>

### 一轮是怎么走完的

验证者每一轮都被要求给出一个固定结构的结论：

```python
class _VerificationResult(BaseModel):
    passed: bool     # 是否通过
    message: str     # 未通过时，说明缺什么、要改什么
```

流水线用结构化输出拿到它，因此验证者在给结论之前可以随意调用工具 —— 读代码、跑测试、查接口，都在它自己的推理循环里完成，流水线只关心最后那个结论。不通过时，`message` 会被**原样**转交给执行者，所以它必须写清楚**缺什么**，而不是说「有问题」。

<Steps>
  <Step title="执行者工作">
    执行者收到任务开始干活，产出留在共享工作区里。
  </Step>
  <Step title="验证者判定">
    验证者带着 `goal` 检查产出，最后给出 `passed` 与 `message`。
  </Step>
  <Step title="通过则结束">
    `passed=True` —— 流水线结束，事件流关闭。
  </Step>
  <Step title="不通过则退回">
    `message` 包在一段提醒里交给执行者，回到第一步重做。
  </Step>
</Steps>

两种「重来」性质不同，只有第一种消耗 `max_iters`：

| 情况 | 含义 | 是否消耗 `max_iters` |
|------|------|---------------------|
| 验证者判定不通过 | 活儿确实没做好 | **是** |
| 验证者没给出合法的结构化结论 | 模型没照要求调工具 | 否，提醒后重问 |

第二种是故障不是判决，算进预算会让执行者平白少几次机会。达到 `max_iters` 后流水线停止 —— 此时事件流结束，最后一次 `message` 留在验证者的对话里。

## 中断与恢复

执行者或验证者停在工具授权上时，`reply_stream` **直接结束**，不占用协程、不持有锁、不轮询等待。把结果重新喂回来即可继续：

```python
async for event in pipe.reply_stream(user_msg):
    ...   # 中途出现 RequireUserConfirmEvent，流随即结束

async for event in pipe.reply_stream(user_confirm_result_event):
    ...   # 从中断处接着跑
```

**恢复时不需要说明该找谁。** 事件自带的 `reply_id` 指明了当时是哪一方被挂起，流水线据此把结果交给对应的智能体。

`reply_stream` 接受的输入：

| 输入类型 | 含义 |
|----------|------|
| `Msg` / `list[Msg]` | 开始一次新的运行，迭代预算重新计数 |
| `UserConfirmResultEvent` | 用户对工具授权的答复 |
| `ExternalExecutionResultEvent` | 外部执行的结果 |
| `UserInterruptEvent` | 放弃当前挂起的调用，整个流水线随之结束 |

<Note>
迭代预算记在流水线实例上，而不是 `reply_stream` 的局部变量里 —— 中断恢复不会让已经用掉的次数回满。
</Note>

## 在终端 UI 中调试

调一条流水线最省事的方式，是把它整个交给[终端 UI](/versions/2.0.8dev/zh/building-blocks/console)，不需要任何适配代码：

```python
await launch_console(agent=pipe)
```

跑起来之后：

| 你会看到 | 由谁负责 |
|----------|----------|
| 两个智能体轮流发言，各自的思考、工具调用与结果都在同一个流里 | 流水线统一吐出内部事件，用 `reply_id` 区分是谁 |
| 工具授权提示（`y` 允许一次，`a` 连同建议规则一起接受） | 终端 UI 负责询问，`reply_id` 保证答复回到正确的那个智能体 |
| `Ctrl+C` 中断当前回复，`exit` / `quit` / `Ctrl+D` 退出 | 终端 UI |

在确认提示上按 `Ctrl+D` 会发出 `UserInterruptEvent`：被挂起的那个智能体关掉未决的工具调用，整条流水线随之结束 —— 中断是**放弃**，不是继续。
